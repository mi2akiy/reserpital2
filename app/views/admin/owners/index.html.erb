<div class="d-flex justify-content-start">
  <%= render 'owners', url: admin_hospital_owners_path, url2: admin_hospital_owner_path(@hospital.id,owner.id) %>
  
  <div class="d-flex justify-content-around">
    <div class="m-2">
      <%= link_to "戻る", admin_hospital_path(@hospital), class:"btn btn-light" %>
    </div>
  </div>
</div>